export const firstName = 'firstName'

export function getName ( x ) {
    return x.a
}

getName({ a: 123 })

function app ( x ) {
    return x * x * x;
}

export default app;

// if(/Edg1/.test(window.navigator.userAgent)){
//
// }
